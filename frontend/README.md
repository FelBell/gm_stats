# TTT Stats Frontend (Stub)

This is a placeholder frontend service served by Nginx.

## Development

Currently, it only serves a static `index.html`.
Future development will replace this with a modern JS framework (e.g., React, Vue) to visualize data from the backend API.
